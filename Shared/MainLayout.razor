@inherits LayoutComponentBase
@inject IDialogService DialogService


@* <MudThemeProvider @bind-IsDarkMode="@themeOn"/> *@
<MudThemeProvider Theme="@_currentTheme"/>
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout class="p-8">
    <MudAppBar Elevation="1" >
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        <MudText Typo="Typo.h5" Class="ml-3">Application</MudText>
        <MudSpacer />
        <MudSpacer />

        <Cart />
        
        <MudAvatar Color="Color.Secondary" Class="ml-9">YV</MudAvatar>
        @* <MudIconButton Icon="@Icons.Material.Filled.MoreVert" Color="Color.Inherit" Edge="Edge.End" /> *@
        <MudAvatar Color="Color.Secondary" Class="ml-2">
            <MudIconButton Icon="@themIcon"  OnClick="ChangeThemeAsync"/>
        </MudAvatar>
    </MudAppBar>
    <MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always"  >
        <NavMenu/>
    </MudDrawer>
    <MudMainContent Class="mt-4 px-8">
        @Body
    </MudMainContent>
</MudLayout>


@code {

   


    bool themeOn = true;

    @* bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }

    MudTheme *@
}